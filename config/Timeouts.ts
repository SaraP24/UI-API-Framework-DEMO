export const DEFAULT_TIMEOUT = 60 * 1000;
export const SHORT_TIMEOUT = 5 * 1000;
export const LONG_TIMEOUT = 120 * 1000;

export const TIMEOUTS = {
    DEFAULT_TIMEOUT,
    SHORT_TIMEOUT,
    LONG_TIMEOUT
} as const;